import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dimovdmitrii.vokabeln',
  appName: 'English Vocabulary Trainer',
  webDir: 'dist'
};

export default config;
